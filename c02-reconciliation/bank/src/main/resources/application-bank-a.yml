# 付款行（bank-a / ICBC）profile
server:
  port: 18081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/gyd_c02_bank_a
    username: gyd
    password: gyd123
    driver-class-name: org.postgresql.Driver

gyd:
  c02:
    bank:
      code: ICBC
      queue: gyd.c02.settlement.bank-a